const myobj = {
    js : 'javascript',
    cpp : 'C++',
    rb : 'ruby',
//     python : 'python'
// }

// for (const key in myobj) {
//    console.log(`${key}full form is    ${myobj[key]}`    );
   
}

const arr = ['js','py','sw','rb']
for (const key in arr) {
    // console.log(key)
}

const map = new Map()
map.set('in','91')
map.set('fr','92')
map.set('uk','91')
map.set('uk','91')

for (const key in map) {
   console.log(key);
   
}